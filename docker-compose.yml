version: '3.9'

services:

  postgres:
    image: postgres:16
    container_name: ump-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: user_management
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  mongodb:
    image: mongo:7
    container_name: ump-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:7
    container_name: ump-redis
    restart: unless-stopped
    ports:
      - "6379:6379"

  rabbitmq:
    image: rabbitmq:3-management
    container_name: ump-rabbitmq
    restart: unless-stopped
    ports:
      - "5672:5672"
      - "15672:15672"

  qdrant:
    image: qdrant/qdrant
    container_name: ump-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  api-gateway:
    build:
      context: ./services/api-gateway
    container_name: ump-api-gateway
    restart: unless-stopped
    env_file:
      - ./services/api-gateway/.env
    ports:
      - "3000:3000"
    volumes:
      - ./services/api-gateway:/app
      - /app/node_modules
    depends_on:
      - auth-service
      - user-service

  auth-service:
    build:
      context: .
      dockerfile: ./services/auth-service/Dockerfile
    container_name: ump-auth-service
    restart: unless-stopped
    env_file:
      - ./services/auth-service/.env
    ports:
      - "3002:3002"
    volumes:
      - ./services/auth-service:/app
      - /app/node_modules
    depends_on:
      - postgres
      - redis
      - rabbitmq

  user-service:
    build:
      context: .
      dockerfile: ./services/user-service/Dockerfile
    container_name: ump-user-service
    restart: unless-stopped
    env_file:
      - ./services/user-service/.env
    ports:
      - "3003:3003"
    volumes:
      - ./services/user-service:/app
      - /app/node_modules
    depends_on:
      - postgres
      - rabbitmq

  audit-service:
    build:
      context: .
      dockerfile: ./services/audit-service/Dockerfile
    container_name: ump-audit-service
    restart: unless-stopped
    env_file:
      - ./services/audit-service/.env
    ports:
      - "3001:3001"
    volumes:
      - ./services/audit-service:/app
      - /app/node_modules
    depends_on:
      - mongodb
      - rabbitmq

  web-app:
    build:
      context: ./frontend/web-app
    container_name: ump-web-app
    restart: unless-stopped
    env_file:
      - ./frontend/web-app/.env
    ports:
      - "5173:5173"
    volumes:
      - ./frontend/web-app:/app
      - /app/node_modules
    depends_on:
      - api-gateway

volumes:
  postgres_data:
  mongodb_data:
  qdrant_data:
